<% name ||= "Crear" %>
<h1><%= name %> Cliente</h1>
<% @client.errors %>
	<%= semantic_form_for @client do |f| %>
	<%= f.input :nombre, placeholder: "Nombre del cliente aquí" %>
	<%= f.input :apellido, placeholder: "Apellido" %>
	<%= f.input :identificacion, placeholder: "Identificacion" %>
	<%= f.input :email, placeholder: "Email" %>
	<%= f.input :telefono, placeholder: "Teléfono" %>
	<%= f.input :direccion, placeholder: "Dirección" %>
	<%= f.input :descripcion, placeholder: "Descripción" %>
	<%= f.input :tipo, :as => :select, :collection => Client.tipos.keys.to_a %>
	<%= f.input :frecuencia, :as => :select, :collection => Client.frecuencia.keys.to_a %>
	<%= f.submit "Guardar", :class => 'text-green be-transparent font-medium' %>
	<%= link_to "Cancelar", "/clients", :class => 'text-red font-medium' %> 
	<% end %>	